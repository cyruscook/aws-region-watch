[project]
name = "aws-data-tracker"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.16.6",
    "ty>=0.0.78",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
extend-select = ["I", "UP"]
